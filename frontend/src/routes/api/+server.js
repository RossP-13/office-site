import { json } from '@sveltejs/kit';

/* sample of how to differentiate call actions */
/* export async function POST({ request }) {
  const action = new URL(request.url).searchParams.get('action');
  
  if (action === 'validate') {
    // validation logic
  } else if (action === 'upload') {
    // upload logic
  }
} */

  

  